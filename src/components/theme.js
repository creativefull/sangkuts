import {StyleSheet} from 'react-native'

const Color = {
    primary : '#1C2A39',
    light : '#3C4A59',
    default : '#FFF'
}

const Theme = StyleSheet.create({

})

module.exports = {
    Theme : Theme,
    Color : Color
}